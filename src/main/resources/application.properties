# Phase 1.1: Minimal configuration for health check endpoint

# Server configuration
server.port=8080

# Logging
logging.level.org.springframework.web=INFO
logging.level.ez.backend.turbo=DEBUG

# Disable database features not needed in Phase 1.1
spring.sql.init.mode=never
spring.jpa.hibernate.ddl-auto=none
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration,org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration

# Database configuration (commented for Phase 1.1, enabled in Phase 1.2)
#spring.datasource.url=jdbc:postgresql://localhost:5432/ezdb
#spring.datasource.username=postgres
#spring.datasource.password=yourpassword
#spring.datasource.driver-class-name=org.postgresql.Driver
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
#logging.level.org.springframework.jdbc.core=DEBUG
#logging.level.org.hibernate.SQL=DEBUG

# MATSim paths (commented for Phase 1.1, used in Phase 3+)
#matsim.input-directory=src/main/resources/matsim-input
#matsim.indexes-directory=src/main/resources/indexes
